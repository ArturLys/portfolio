import MinecraftMenu from "@/components/MinecraftMenu";

export default function Home() {
  return (
    <main className="fixed inset-0 w-full h-[100dvh] overflow-hidden select-none touch-none bg-transparent">
      <MinecraftMenu />
    </main>
  );
}
